apiVersion: v1
kind: Service
metadata:
  name: {{ include "confapi.fullname" . }}
  labels:
    {{- include "confapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "confapi.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
